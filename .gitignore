# ── OS cruft ───────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
*.icloud
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
.eggs/
.python-version
.venv/
venv/
.env
.env.*
!.env.example
.envrc
*.log
.mypy_cache/
.pytype/
.pytest_cache/
.pyre/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# ── Node / Frontend (Astro + Vite) ─────────────────────────────────────────────
node_modules/
dist/
build/
.astro/
.vite/
.parcel-cache/
*.local

# ── Yarn Berry (PnP) ──────────────────────────────────────────────────────────
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz
# keep these folders if present:
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
# keep PnP artifacts (should be committed)
!.pnp.cjs
!.pnp.loader.mjs

# Legacy/npm logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Docker / Infra ────────────────────────────────────────────────────────────
infra/compose/*.env
infra/compose/.env.local
# If you ever bind-mount data dirs locally, ignore them here:
infra/compose/dbdata/
infra/compose/pgdata/
docker-compose.override.yml

# ── Postgres dumps (keep schema in database/scripts/) ─────────────────────────
*.dump
*.sql.gz
dump.sql

# ── Editors / IDE ─────────────────────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# ── Misc ──────────────────────────────────────────────────────────────────────
*.swp
*.swo
